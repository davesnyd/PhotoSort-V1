# PhotoSort Docker Environment Configuration Template
# Copyright 2025, David Snyderman
#
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.docker.template .env
# 2. Fill in all the values marked with YOUR_*_HERE
# 3. NEVER commit the .env file to git (it's in .gitignore)
# 4. Keep your .env file secure - it contains sensitive credentials

# ============================================
# Database Configuration
# ============================================
DB_NAME=PhotoSortData
DB_USERNAME=photosort_user
DB_PASSWORD=YOUR_SECURE_DATABASE_PASSWORD_HERE
DB_PORT=5432

# ============================================
# OAuth 2.0 Configuration (Google)
# ============================================
# Get these from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID (Web application)
# Authorized JavaScript origins: http://localhost:8080
# Authorized redirect URIs: http://localhost:8080/login/oauth2/code/google

OAUTH_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE
OAUTH_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET_HERE
OAUTH_REDIRECT_URI=http://localhost:8080/login/oauth2/code/google

# ============================================
# Git Repository Configuration
# ============================================
# Path inside the container where photos are stored
GIT_REPO_PATH=/data/photos

# Git repository URL (optional - for Git polling feature)
# Example: https://github.com/username/photo-repo.git
GIT_REPO_URL=YOUR_GIT_REPO_URL_HERE_OR_LEAVE_EMPTY

# Git credentials (optional - only if using Git polling)
GIT_USERNAME=YOUR_GITHUB_USERNAME_HERE_OR_LEAVE_EMPTY
GIT_TOKEN=YOUR_GITHUB_PERSONAL_ACCESS_TOKEN_HERE_OR_LEAVE_EMPTY

# ============================================
# Backend Configuration
# ============================================
BACKEND_PORT=8080
SPRING_PROFILES_ACTIVE=prod

# JPA/Hibernate DDL mode
# Options: none, validate, update, create, create-drop
# Use 'update' for development, 'validate' for production
JPA_DDL_AUTO=update

# Show SQL queries in logs (true/false)
# Set to false in production for better performance
JPA_SHOW_SQL=false

# Java memory settings
# Adjust based on available server resources
JAVA_OPTS=-Xmx512m -Xms256m

# ============================================
# Frontend Configuration
# ============================================
FRONTEND_PORT=3000

# API Base URL (how frontend connects to backend)
# For local Docker: http://localhost:8080
# For production: https://your-domain.com
REACT_APP_API_BASE_URL=http://localhost:8080

# ============================================
# Additional Configuration
# ============================================
# Docker Compose project name
COMPOSE_PROJECT_NAME=photosort
